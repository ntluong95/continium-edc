{{- if .Values.envoy.enabled }}
{{- if and (not .Values.envoy.continium.gatewayClass.create) (not .Values.envoy.continium.gatewayClass.name) -}}
{{- fail "envoy.continium.gatewayClass.name is required when envoy.continium.gatewayClass.create=false" -}}
{{- end -}}
{{- $namespace := include "continium.namespace" . -}}
{{- $gatewayName := include "continium.envoy.gatewayName" . -}}
{{- $gatewayClassName := include "continium.envoy.gatewayClassName" . -}}
{{- $proxyName := include "continium.envoy.proxyName" . -}}
{{- $proxyServiceName := include "continium.envoy.proxyServiceName" . -}}
{{- $appServiceName := include "continium.name" . -}}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: {{ $proxyName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyDeployment:
        replicas: {{ .Values.envoy.continium.proxy.replicas }}
      envoyService:
        name: {{ $proxyServiceName }}
        type: {{ .Values.envoy.continium.proxy.service.type }}
        labels:
          app.kubernetes.io/name: {{ $proxyServiceName }}
          app.kubernetes.io/managed-by: {{ .Release.Service }}
        {{- if .Values.envoy.continium.proxy.service.annotations }}
        annotations:
          {{- include "continium.tplvalues.render" (dict "value" .Values.envoy.continium.proxy.service.annotations "context" $ ) | nindent 10 }}
        {{- end }}
  logging:
    level:
      default: {{ .Values.envoy.continium.proxy.logging.defaultLevel | quote }}
  telemetry:
    metrics:
      prometheus:
        disable: {{ not .Values.envoy.continium.proxy.telemetry.prometheus.enabled }}
    {{- if .Values.envoy.continium.proxy.telemetry.openTelemetry.enabled }}
      sinks:
        - type: OpenTelemetry
          openTelemetry:
            {{- if .Values.envoy.continium.proxy.telemetry.openTelemetry.host }}
            host: {{ .Values.envoy.continium.proxy.telemetry.openTelemetry.host | quote }}
            port: {{ .Values.envoy.continium.proxy.telemetry.openTelemetry.port }}
            {{- else }}
            backendRefs:
              {{- toYaml .Values.envoy.continium.proxy.telemetry.openTelemetry.backendRefs | nindent 14 }}
            {{- end }}
            resourceAttributes:
              service.name: {{ default $proxyServiceName .Values.envoy.continium.proxy.telemetry.openTelemetry.serviceName | quote }}
              service.namespace: {{ default $namespace .Values.envoy.continium.proxy.telemetry.openTelemetry.serviceNamespace | quote }}
              deployment.environment: {{ default "self-hosted" .Values.envoy.continium.proxy.telemetry.openTelemetry.deploymentEnvironment | quote }}
    {{- end }}
  preserveRouteOrder: true
{{- if .Values.envoy.continium.gatewayClass.create }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ $gatewayClassName }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  controllerName: {{ .Values.envoy.config.envoyGateway.gateway.controllerName | quote }}
  parametersRef:
    group: gateway.envoyproxy.io
    kind: EnvoyProxy
    name: {{ $proxyName }}
    namespace: {{ $namespace }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ $gatewayName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  gatewayClassName: {{ $gatewayClassName }}
  listeners:
    - name: {{ .Values.envoy.continium.gateway.listenerName }}
      protocol: HTTP
      port: {{ .Values.envoy.continium.gateway.port }}
      allowedRoutes:
        namespaces:
          from: Same
{{- if .Values.envoy.continium.clientTrafficPolicy.enabled }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: {{ printf "%s-client-ip" $gatewayName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $gatewayName }}
  clientIPDetection:
    xForwardedFor:
      numTrustedHops: {{ .Values.envoy.continium.clientTrafficPolicy.numTrustedHops }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-health" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.continium.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: Exact
            value: /health
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- if .Values.envoy.continium.routes.authCallback }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-auth-callback" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.continium.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/auth/callback
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.continium.routes.v1Client }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v1-client" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.continium.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/client
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.continium.routes.v2Client }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v2-client" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.continium.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v2/client
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.continium.routes.v1Management }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v1-management" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.continium.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/management
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.continium.routes.storage }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-storage" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "continium.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.continium.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /storage
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- end }}
